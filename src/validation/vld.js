export default {
    rootConfig: {
        type: '',
        version: '',
    },
    validate(obj, path = '/', config = null) {
        let result = [];
        const c = config ?? this.rootConfig;


        return result;
    },
    check() {

    },
}
